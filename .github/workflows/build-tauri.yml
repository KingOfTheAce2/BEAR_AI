# Legacy Workflow - Replaced by ci-cd.yml
# This workflow is kept for reference and will be removed in future versions
# Please use the comprehensive ci-cd.yml workflow instead

name: "[DEPRECATED] Build BEAR AI Desktop App"

on:
  # Disabled - use ci-cd.yml instead
  workflow_dispatch:
    inputs:
      legacy_build:
        description: 'Run legacy build (use ci-cd.yml instead)'
        required: true
        default: 'false'

jobs:
  deprecation-notice:
    name: Deprecation Notice
    runs-on: ubuntu-latest
    steps:
      - name: Deprecation Warning
        run: |
          echo "⚠️ This workflow is deprecated!"
          echo "Please use the comprehensive ci-cd.yml workflow instead."
          echo "The new workflow provides:"
          echo "- Cross-platform builds (Windows, macOS, Linux)"
          echo "- Automated testing"
          echo "- Security scanning"
          echo "- Better caching"
          echo "- Release automation"
          exit 1