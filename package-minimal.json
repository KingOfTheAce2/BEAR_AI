{
  "name": "bear-ai-gui",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.6.0"
  },
  "scripts": {
    "build": "echo Building... && mkdir -p build && cp src/index.html build/ 2>/dev/null || echo Done",
    "tauri:build": "tauri build"
  }
}
